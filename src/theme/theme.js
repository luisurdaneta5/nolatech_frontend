import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#1976d2",
        },
        secondary: {
            main: "#adc8f4",
        },
        custom: {
            main: "#000000",
        },
        navbar: {
            main: "#f7f6f2",
        },
    },
});
